/* Implementation of functions described in arith.h */

/* <arith.c>
 *  <includes>
 *  <functions>
 */

int Arith_max(int x, int y) {
    return x > y ? x : y;
}

int Arith_min(int x, int y) {
    return x > y ? y : x;
}
